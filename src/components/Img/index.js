import React from 'react';

const Img = ({ image, src, alt, className, parentKey, i }) => {
 
  return (
    
    <img
        src={src}
        alt={alt}
        className={className}
        

    />
    
  );
};

export default Img;
